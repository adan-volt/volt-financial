<!-- Hero section -->
<div class="section-fullscreen bg-image vw-100 d-none-lg" data-bg-src="{{ asset($img) }}">
    <div class="banner-flex-sm">
        <div class="flex-column">
            <h1 class="banner-title-sm {{ $text_color }}">{{ $title }}<span class="blue-dot">.</span></h1>
            <p class="banner-subtitle-sm {{ $text_color }}">
                {{ $subtitle }}
            </p>
        </div>
    </div>
</div>
<!-- end Hero section -->